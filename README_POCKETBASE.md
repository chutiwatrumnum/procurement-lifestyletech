# 🗄️ PocketBase Setup for Procurement System

**สร้างโดย** หมี 🐻  
**อัปเดตล่าสุด** 2026-02-27

ระบบจัดซื้อนี้รองรับการเชื่อมต่อกับ **PocketBase** เป็น Backend จริงค่ะ เพื่อให้พี่เจมส์สามารถเก็บข้อมูลและ Login ได้แบบ Real-time

---

## 📦 PocketBase ในโปรเจคนี้

โปรเจคนี้มี PocketBase ฝังมาพร้อมในโฟลเดอร์ `pocketbase/` ค่ะ:

```
pocketbase/
├── pocketbase              # Binary file (embedded backend)
├── pb_data/                # Database files (SQLite)
│   ├── data.db            # Main database
│   ├── auxiliary.db        # Auxiliary database
│   └── types.d.ts         # TypeScript types
├── CHANGELOG.md           # PocketBase changelog
└── LICENSE.md             # PocketBase license
```

### วิธีรัน PocketBase (ที่มาพร้อมโปรเจค)

```bash
cd pocketbase
./pocketbase serve
```

เข้าหน้า Admin ที่: http://127.0.0.1:8090/_/

---

## 🚀 วิธีติดตั้ง Backend

### 1. นำเข้า Schema (ล่าสุด)
1. รัน PocketBase ตามคำสั่งข้างต้น
2. เข้าหน้า Admin ที่ http://127.0.0.1:8090/_/
3. สร้าง Admin account ครั้งแรก
4. ไปที่ **Settings > Import collections**
5. กดปุ่ม "Load from JSON file"
6. เลือกไฟล์ `pb_schema.json` (หรือ `pb_schema (2).json`)
7. กด **Import**

### 2. ตั้งค่า API URL ใน Frontend
1. เปิดไฟล์ `.env` ในโฟลเดอร์โปรเจค
2. ตรวจสอบว่า `VITE_POCKETBASE_URL` ตรงกับ URL ของ PocketBase (ปกติคือ `http://127.0.0.1:8090`)

### 3. นำเข้าข้อมูลเริ่มต้น (Seed Mockup Data)
เพื่อให้ระบบไม่ว่างเปล่า หมีเตรียมข้อมูลโครงการและผู้ขายไว้ให้แล้วค่ะ:
1. ในหน้า Admin PocketBase ไปที่ตาราง `projects` และ `vendors`
2. สร้างข้อมูลเริ่มต้นได้เลย

---

## 🔑 การใช้งานระบบ Login

### วิธีที่ 1: สร้าง User จริงใน PocketBase
1. ไปที่ collection `users` ในหน้า Admin
2. กด **New record**
3. กรอก `email`, `password`, `name`, และเลือก `role`
4. ใช้ account นี้ Login เข้าหน้าเว็บได้เลยค่ะ

### วิธีที่ 2: ใช้ Demo (ถ้ายังไม่รัน PocketBase)
- ระบบยังรองรับการ Login แบบ Demo เหมือนเดิมค่ะ โดยใช้:
  - Email: `demo@example.com`
  - Password: `demo1234`
- ข้อมูลที่เห็นจะเป็น Mock Data แต่ถ้าพี่เจมส์รัน PocketBase สำเร็จ ข้อมูลจะถูกดึงจาก Database จริงอัตโนมัติค่ะ

---

## 📊 โครงสร้างตาราง (Collections) - ล่าสุด

### System Collections
| Collection ID | ชื่อ | รายละเอียด |
|---------------|------|------------|
| pbc_3142635823 | `_superusers` | System admin (superuser) |
| pbc_2279338944 | `_mfas` | Multi-factor authentication |
| pbc_1638494021 | `_otps` | One-time passwords |
| pbc_2281828961 | `_externalAuths` | External authentication |
| pbc_4275539003 | `_authOrigins` | Authentication origins |

### Custom Collections
| Collection ID | ชื่อ | รายละเอียด |
|---------------|------|------------|
| _pb_users_auth_ | `users` | พนักงาน/ผู้ใช้งาน |
| pbc_3865025440 | `departments` | แผนก |
| pbc_484305853 | `projects` | โครงการ |
| pbc_461364642 | `project_items` | รายการวัสดุในโครงการ |
| pbc_3732325883 | `vendors` | ผู้ขาย |
| pbc_3482049810 | `purchase_requests` | ใบขอซื้อ (PR) |
| pbc_674198416 | `pr_items` | รายการใน PR |
| pbc_66484338 | `pr_history` | ประวัติการดำเนินการ PR |
| pbc_2301922722 | `notifications` | การแจ้งเตือน |

---

## 📝 ฟิลด์ในแต่ละ Collection

### users (พนักงาน)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| id | text | Primary key |
| email | email | อีเมล (required) |
| password | password | รหัสผ่าน |
| name | text | ชื่อ-นามสกุล |
| avatar | file | รูปโปรไฟล์ |
| role | select | superadmin, head_of_dept, manager, employee |
| is_active | bool | สถานะใช้งาน |
| phone | number | เบอร์โทรศัพท์ |
| department | relation | แผนก |
| manager | relation | หัวหน้า |
| position | text | ตำแหน่ง |
| signature | file | ลายเซ็น |

### departments (แผนก)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| name | text | ชื่อแผนก |
| code | text | รหัสแผนก |
| head | relation | หัวหน้าแผนก |
| description | text | รายละเอียด |

### projects (โครงการ)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| name | text | ชื่อโครงการ (required) |
| code | text | รหัสโครงการ |
| budget | number | งบประมาณ |
| location | text | สถานที่ |
| status | text | สถานะ |

### project_items (รายการวัสดุโครงการ)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| project | relation | โครงการ |
| name | text | ชื่อรายการ |
| unit | text | หน่วย |
| quantity | number | จำนวน |
| unit_price | number | ราคาต่อหน่วย |
| total_price | number | ราคารวม |
| initial_quantity | number | จำนวนเริ่มต้น |

### vendors (ผู้ขาย)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| name | text | ชื่อผู้ขาย (required) |
| tax_id | text | เลขภาษี |
| category | text | ประเภท |
| address | text | ที่อยู่ |
| contact_person | text | ผู้ติดต่อ (required) |
| email | email | อีเมล |
| phone | text | เบอร์โทรศัพท์ |
| status | text | สถานะ |
| attachments | file | เอกสารแนบ |
| vendor_type | select | domestic, foreign |
| payment_term | select | 30days, 45days, 60days, custom |
| payment_term_detail | text | รายละเอียดการชำระเงิน |

### purchase_requests (ใบขอซื้อ - PR)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| pr_number | text | เลขที่ PR (required) |
| type | text | ประเภท |
| po_ref | text | เลขที่ PO อ้างอิง |
| delivery_location | text | สถานที่ส่งมอบ |
| status | text | สถานะ |
| total_amount | number | จำนวนเงินรวม |
| attachments | file | เอกสารแนบ |
| project | relation | โครงการ |
| vendor | relation | ผู้ขาย |
| requester | relation | ผู้ขอ |
| requester_name | text | ชื่อผู้ขอ |
| rejection_reason | text | เหตุผลที่ปฏิเสธ |
| approved_by | relation | ผู้อนุมัติ |
| approved_at | date | วันที่อนุมัติ |
| approval_level | number | ระดับการอนุมัติ |
| head_of_dept_approved_by | relation | หัวหน้าแผนกอนุมัติ |
| head_of_dept_approved_at | date | วันที่หัวหน้าอนุมัติ |
| head_of_dept_comment | text | ความคิดเห็นหัวหน้า |
| head_of_dept_signature | file | ลายเซ็นหัวหน้า |
| head_of_dept_approved_by_name | text | ชื่อผู้อนุมัติ |
| manager_approved_by | relation | ผู้จัดการอนุมัติ |
| manager_approved_at | date | วันที่ผจ.อนุมัติ |
| manager_signature | file | ลายเซ็นผจ. |
| manager_approved_by_name | text | ชื่อผจ. |
| manager_comment | text | ความคิดเห็นผจ. |

### pr_items (รายการใน PR)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| name | text | ชื่อรายการ (required) |
| unit | text | หน่วย |
| quantity | number | จำนวน |
| unit_price | number | ราคาต่อหน่วย |
| total_price | number | ราคารวม |
| pr | relation | ใบขอซื้อ |
| project_item | relation | รายการในโครงการ |
| item_type | select | regular, reserve |

### pr_history (ประวัติ PR)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| action | text | การกระทำ |
| by | relation | ผู้กระทำ |
| pr | relation | ใบขอซื้อ |
| details | text | รายละเอียด |
| old_attachments | json | ไฟล์แนบเดิม |

### notifications (การแจ้งเตือน)
| ฟิลด์ | ประเภท | รายละเอียด |
|-------|--------|------------|
| user | relation | ผู้ใช้ |
| title | text | หัวข้อ |
| message | text | ข้อความ |
| type | select | approval, rejection, info |
| pr_id | text | ID ของ PR |
| is_read | bool | อ่านแล้วหรือยัง |

---

## ⚙️ การตั้งค่าเพิ่มเติม

### Environment Variables
สร้างไฟล์ `.env` หรือแก้ไขในโฟลเดอร์โปรเจค:

```env
VITE_POCKETBASE_URL=http://127.0.0.1:8090
```

### API Client
PocketBase client ถูกตั้งค่าใน `src/lib/pocketbase.ts` ค่ะ:

```typescript
import PocketBase from 'pocketbase';

export const pb = new PocketBase(import.meta.env.VITE_POCKETBASE_URL);
```

---

## 🔧 Troubleshooting

### ถ้าเข้าไม่ได้
- ตรวจสอบว่า PocketBase กำลังรันอยู่: `lsof -i :8090`
- ลอง restart: `./pocketbase serve`

### ถ้าข้อมูลไม่แสดง
- ตรวจสอบ `.env` ว่าตรงกับ URL ของ PocketBase
- ดู Console ใน Browser ว่ามี error หรือไม่
- ตรวจสอบ Rules ในแต่ละ collection ว่าถูกต้อง

ถ้าพี่เจมส์ติดขัดตรงไหนในการตั้งค่า บอกหมีได้เลยนะคะ! ✨🐻
